<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.KG.mybatis.myMapper">

	<!-- 회원 목록 조회 -->
	<select id="selectMemberList" resultType="memberDTO">
		SELECT * FROM member
	</select>

	<!-- 회원 목록 관리 > 추가 -->
	<insert id="insertMember">
		INSERT INTO member VALUES(#{m_id}, #{m_pw},
		#{m_name}, #{m_nick}, #{m_birth}, #{m_email})
	</insert>

	<!-- 회원 목록 관리 > 수정 -->
	<update id="updateMember">
		UPDATE member SET m_pw = #{m_pw}, m_name = #{m_name},
		m_nick = #{m_nick}, m_birth = #{m_birth} WHERE m_id = #{m_id}
	</update>

	<!-- 회원 목록 관리 > 삭제 -->
	<update id="deleteMember">
		DELETE FROM member WHERE m_id = #{m_id}
	</update>

	<!-- category 추가 -->
	<insert id="inscategory" parameterType="boardDTO">
		INSERT INTO BOARD(B_CATEGORY,B_CATENUM) values(#{b_category},(SELECT
		MAX(b_catenum) from BOARD)+1)
	</insert>

	<!-- category 수정 -->
	<update id="updcategory" parameterType="boardDTO">
		UPDATE BOARD SET
		B_CATEGORY = #{b_title} where B_CATEGORY = #{b_category}
	</update>

	<!-- category 삭제 -->
	<update id="delcategory" parameterType="boardDTO">
		DELETE FROM BOARD WHERE B_CATEGORY = #{b_category}
	</update>

	<!-- article 추가 -->
	<insert id="insarticle" parameterType="boardDTO">
		INSERT INTO BOARD(B_CATEGORY,B_ARTICLE,B_ARTICLENUM) values(#{b_category},
		#{b_article},(SELECT MAX(B_ARTICLENUM) from BOARD WHERE b_category =
		#{b_category})+1)
	</insert>

	<!-- article 수정 -->
	<update id="updarticle" parameterType="boardDTO">
		UPDATE BOARD SET B_ARTICLE
		= #{b_title} where B_ARTICLE = #{b_article}
	</update>

	<!-- article 삭제 -->
	<update id="delarticle" parameterType="boardDTO">
		DELETE FROM BOARD WHERE B_ARTICLE = #{b_article}
	</update>

	<!-- PAGING > 총 인원 수 -->
	<select id="getMemberCount">
		SELECT COUNT(*) FROM member
	</select>

	<!-- PAGING > 각 페이지별로 인원 출력 -->
	<update id="pagingCount">
		SELECT B.* FROM
		(SELECT ROWNUM m_rn, A.* FROM
		(SELECT *
		FROM MEMBER ORDER BY M_RANK, M_BIRTH)A)B
		WHERE m_rn BETWEEN #{startNum}
		AND #{lastNum};
	</update>
</mapper>