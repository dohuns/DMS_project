<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.KG.mybatis.myMapper">

	<!-- 댓글 저장 -->
	<insert id="commentSave">
		INSERT INTO reply values(#{c_nick}, sysdate, #{c_content}, #{c_num}, 0, c_numSQ.nextval , #{c_id})	
	</insert>
	
	<!-- 댓글 리스트 -->
	<select id="commentList" resultType="commentDTO">
		SELECT * FROM reply WHERE c_num = #{c_num}
			ORDER BY c_group ASC , c_renum ASC
	</select>
	
	<!-- 댓글 개수 -->
	<select id="getCount" resultType="int">
		SELECT count(*) FROM reply WHERE c_num = #{c_num}
	</select>
	
</mapper>